<% content_for :title, "Edit #{@link.title} · #{@profile.name}" %>

<%= render "shared/background", seed: @profile.id %>

<div class="panel">
  <h1>Edit link</h1>
  <%= render "links/form", profile: @profile, link: @link,
             url: profile_link_path(@profile, @link), method: :patch, submit: "Save" %>
</div>
