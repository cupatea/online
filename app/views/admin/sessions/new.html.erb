<% content_for :title, "Admin sign in" %>
<% content_for :body_class, "auth-page" %>

<div class="auth-card">
  <div class="auth-mark" aria-hidden="true">
    <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round">
      <rect x="4" y="10" width="16" height="11" rx="3"></rect>
      <path d="M8 10V7a4 4 0 0 1 8 0v3"></path>
      <path d="M12 15v2"></path>
    </svg>
  </div>

  <div class="auth-heading">
    <h1>Welcome back</h1>
    <p>Sign in to manage your services and Caddy configuration.</p>
  </div>

  <% unless AdminPassword.configured? %>
    <div class="flash alert">
      Set <span class="mono">ADMIN_PASSWORD_DIGEST</span> (or its <span class="mono">_FILE</span> variant) before signing in.
    </div>
  <% end %>

  <%= form_with url: admin_login_path, class: "stack" do |form| %>
    <div class="auth-field">
      <%= form.label :password, "Password" %>
      <div class="password-input">
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
          <rect x="4" y="10" width="16" height="11" rx="3"></rect>
          <path d="M8 10V7a4 4 0 0 1 8 0v3"></path>
        </svg>
        <%= form.password_field :password, required: true, autofocus: true, autocomplete: "current-password", placeholder: "Enter your admin password" %>
      </div>
    </div>

    <div class="auth-actions">
      <%= form.submit "Sign in", disabled: !AdminPassword.configured? %>
    </div>
  <% end %>
</div>
