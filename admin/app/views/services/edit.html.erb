<% content_for :title, "Edit #{@service.hostname}" %>

<h1 class="mb-6 text-xl font-semibold tracking-tight">Edit <span class="font-mono text-base"><%= @service.hostname %></span></h1>

<%= render "form", service: @service %>
